import React from 'react';
import { Image, TouchableOpacity } from 'react-native';

const AccountButton = ({callback, site}) => (
  <TouchableOpacity className='w-12 h-12 bg-buttonBg rounded-lg'
  activeOpacity={0.7} >

  </TouchableOpacity>
)

export default AccountButton